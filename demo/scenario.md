# Demo — superseded

The canonical, verified demo materials are:
- **[`SCRIPT.md`](SCRIPT.md)** — the second-by-second ≤3-minute script (on-screen text + voiceover),
  every number produced live by the driver.
- **[`run_demo.sh`](run_demo.sh)** — the reproducible, idempotent driver (3 beats).
- **[`TITLE_CARDS.md`](TITLE_CARDS.md)** + `cards/` — brand title cards & thumbnail.

This file used to hold an early outline with private-data figures (30k+ matches, aggregate context)
that the reproducible public demo does not reproduce. It's kept only as a redirect so links don't break.
