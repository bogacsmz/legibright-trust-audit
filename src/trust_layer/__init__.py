"""Statistical Trust Layer Agent.

Reads DataHub via the MCP server, runs statistical data-health checks (Sentinel)
and honest-metrics audits (Auditor), then writes verdicts back to the graph.
"""

__version__ = "0.1.0"
